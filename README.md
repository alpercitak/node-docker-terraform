# node-docker-terraform
Node.js &amp; Nginx deploy on Docker with Terraform

Initialize terraform with brew
```bash
  $ make init
```

Create the plan
```bash
  $ make plan
```

Apply the plan
```bash
  $ make apply
```

Destroy resources
```
  $ make destroy
```
